import { ServerApplication } from './application';
import { ApplicationConfig } from '@loopback/core';
export { ServerApplication };
export declare function main(options?: ApplicationConfig): Promise<ServerApplication>;
